{
  "name": "hobby_lobby_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db_init": "psql -U postgres -f DB/schema.sql",
    "db_seed": "psql -U postgres -f DB/seed.sql",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg-promise": "^11.5.4"
  }
}